# 달팽이는 올라가고 싶다 
import sys  # 시간 제한이 0.15초여서 입력을 빨리 받기 위해 sys사용
a, b, c = map(int, sys.stdin.readline().rstrip().split())
if a >= c:print(1);quit() # 첫 번째 값이 세번 째 값보다 크면 하루에 다 올라가니 1로 처리 후 종료

c-=a # 첫 번째 값이 세번 째 값보다 작을 때 c -= a 낮에 한번에 올라가는걸 계산

if c % (a-b) == 0: # 낮에 올라가고 밤에 내려가는 길이(총 하루 길이)가 c의 약수라면 + 1 을 해줘서 낮에 한번에 올라가는 거 계산
    print(c//(a-b)+1)
elif c % (a-b) != 0: # 약수가 아니라면 + 2
    print(c//(a-b)+2)
