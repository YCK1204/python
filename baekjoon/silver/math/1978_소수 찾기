# 소수 찾기
n = int(input())
data = list(map(int, input().split()))

# 1은 소수도 합성수도 아니기 때문에 리스트 값내 1제거
for i in range(data.count(1)):
    data.remove(1)
result = 0
for i in data:
    cnt = 0
    for j in range(2, i):
        if i % j != 0:cnt+=1 # 2부터 자기 자신 직전까지의 숫자들로 안나누어질 때 카운트
        
    if cnt == i-2:result+=1  # 모두 안나누어졌다면 카운트
print(result)
