# 설탕 배달
n = int(input())
result = 0
# 5의 배수이면 출력 후 종료
if n % 5 == 0:
    print(n//5)
    quit()
    
for i in range((n//5)*5, 0, -5):   # n 값을 나눌 수 있는 가장 큰 5의 배수 부터 0직전까지 

    if (n-i) % 3 == 0:  # 가장 많이 나누어 5로 질때
        result += i//5
        n -= 5*result      
        break


if n % 3 != 0:   # n 값이 5로 나누었는데도 3의 배수가 아니라면
    print(-1)
else:
    print(result+n//3)
