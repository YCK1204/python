# 연속  XOR

# xor는 직접 1부터 15까지 나열하고 xor해보면 규칙을 찾을 수 있음
a, b = map(int, input().split())

def f(n):
    start = n//4 * 4
    answer = 0
    for i in range(start, n+1):
        answer ^= i
    return answer
print(f(a-1) ^ f(b))
